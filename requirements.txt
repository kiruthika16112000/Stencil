customtkinter>=6.0.0
Pillow>=10.0.0
pyserial>=3.5
pymodbus>=3.5
numpy>=1.24

# Optional: only needed to drive a physical Basler camera for the live feed
# on the Main screen. The app degrades gracefully (shows "Camera not
# available") if this isn't installed.
# pypylon>=3.0
